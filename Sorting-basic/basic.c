#include <stdio.h>
int main()
{   int Arr_size ;
    printf("Enter the size of Array -");
    scanf("%d", &Arr_size) ;

    int Arr_unsorted [Arr_size] , Arr_index , provisional_max , provisional_index ;
    printf("\nEnter elements in the Array -") ;
    for(Arr_index = 0 ; Arr_index < Arr_size ; Arr_index ++)
    scanf("%d", &Arr_unsorted [Arr_index]) ;

    //Sorting the Array
    for (Arr_index = 0 ; Arr_index < Arr_size ; Arr_index ++)
    {
        provisional_max = Arr_unsorted[0] ;
        while(Arr)

    }

}